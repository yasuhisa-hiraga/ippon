// **********************************************************
// キュー JacaScript & Node.js
// hiraga 2014/4/3
// hiraga 2015/9/18 特定のタスクを破棄するメソッド追加
// **********************************************************

//
// Queue (FIFO)
//

function Queue() {
    this.__a = new Array();
}

Queue.prototype.enqueue = function(o) {
    this.__a.push(o);
};

Queue.prototype.dequeue = function() {
    if( this.__a.length > 0 ) {
        return this.__a.shift();
    }
    return null;
};

Queue.prototype.size = function() {
    return this.__a.length;
};

Queue.prototype.toString = function() {
    return '[' + this.__a.join(',') + ']';
};

Queue.prototype.removeTask = function( source ){
	var self = this;
	for(var i=0;i<self.__a.length;i++){
		if(__a[i] == source){
			__a.splice(i,1);
		}
	}
};

module.exports = Queue;


